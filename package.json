{
  "name": "create-turbostart-app",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "create-turbostart-app": "dist/create-turbostart-app"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "test": "bun test",
    "build": "bun build src/create-turbostart-app.ts --compile --outfile dist/create-turbostart-app",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "biome lint ."
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "prettier": "^3.3.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "private": false
}